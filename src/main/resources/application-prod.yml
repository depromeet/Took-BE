spring:
  config:
    activate:
      on-profile: prod

  jpa:
    properties:
      hibernate:
        show_sql: false
        use_sql_comments: false
        default_schema: production

webhook:
  slack:
    url: ${SLACK_WEBHOOK_URL}
