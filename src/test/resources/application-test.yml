spring:
  application:
    name: took
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
  data:
    redis:
      host: redis
      port: 6379
      password:

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace

auth:
  access-token-secret: dummy_access_token_secret_dummy_access_token_secret
  access-token-expiration-time: 3600
  refresh-token-expiration-time: 5

oauth: # TODO application.yml로 공통 필드 추출
  kakao:
    auth-code-url: https://kauth.kakao.com/oauth/authorize
    client-id: clientId
    redirect-uri: redirectUri
